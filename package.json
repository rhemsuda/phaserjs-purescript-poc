{
  "scripts": {
    "start": "spago build && parcel index.html",
    "start:docker": "sudo docker run -p 1234:1234 dreamrace-poc:latest",
    "build": "spago build && parcel build index.html",
    "build:docker": "sudo docker build -t dreamrace-poc ."
  },
  "devDependencies": {
    "parcel": "^2.11.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  }
}
